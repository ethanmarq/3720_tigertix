name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install Backend Dependencies
      run: |
        cd backend/admin-service && npm install
        cd ../client-service && npm install
        cd ../user-authentication && npm install
        cd ../llm-service && npm install

    - name: Run Backend Unit Tests
      run: |
        cd backend/admin-service && npm test
        cd ../user-authentication && npm test

    - name: Install Frontend Dependencies
      run: |
        cd frontend && npm install

    - name: Run Frontend Tests
      run: |
        cd frontend && npm test -- --watchAll=false

  # Deployment Job (Conditional)
  deploy:
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Production
        run: echo "Tests passed. Auto-deploy is handled by Vercel/Render automatic git hooks."
